const initilalState = {
    account: {
        username: '',
        password: '',
    },
};

const Reducers = (state = initilalState, action) => {
    switch (action.type) {
        default: return state;
    }
};

export default Reducers;